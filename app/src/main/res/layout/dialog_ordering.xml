<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="300dp"
    android:layout_height="210dp">

    <TextView
        android:id="@+id/textView6"
        android:layout_width="300dp"
        android:layout_height="24dp"
        android:layout_marginTop="16dp"
        android:gravity="center"
        android:text="Оформление заказа"
        style="@style/DefaultDialogTitle"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.302"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <EditText
        android:id="@+id/d_ordering_address"
        android:layout_width="249dp"
        android:layout_height="51dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="24dp"
        android:ems="10"
        android:hint="Адрес"
        android:paddingStart="10dp"
        android:background="@drawable/grey_rect"
        android:inputType="textPersonName"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/textView6" />

    <Button
        android:id="@+id/d_ordering_cancel"
        android:layout_width="108dp"
        android:layout_height="44dp"
        android:layout_marginStart="32dp"
        android:layout_marginTop="24dp"
        style="@style/DefaultButton"
        android:text="Отмена"
        app:layout_constraintStart_toEndOf="@+id/d_ordering_ok"
        app:layout_constraintTop_toBottomOf="@+id/d_ordering_address" />

    <Button
        android:id="@+id/d_ordering_ok"
        android:layout_width="wrap_content"
        android:layout_height="44dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="24dp"
        style="@style/DefaultButton"
        android:text="Оформить"
        android:textSize="12sp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/d_ordering_address" />

</androidx.constraintlayout.widget.ConstraintLayout>